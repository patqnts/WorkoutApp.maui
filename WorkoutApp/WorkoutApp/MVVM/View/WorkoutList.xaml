<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WorkoutApp.MVVM.View.WorkoutList"
             xmlns:models="clr-namespace:WorkoutApp.MVVM.Model"
             xmlns:viewmodel="clr-namespace:WorkoutApp.MVVM.ViewModel"
             x:DataType="viewmodel:WorkoutListViewModel"
             Shell.NavBarIsVisible="False"
             Title="{Binding WorkoutTarget.Name}">
    <ScrollView>
        <StackLayout>
            <Button Text="Add Workout" Command="{Binding AddWorkoutCommand}" HorizontalOptions="End" VerticalOptions="Start" Margin="10"/>
            <CollectionView ItemsSource="{Binding Workout}" SelectionMode="None">
                <CollectionView.ItemsLayout>
                    <GridItemsLayout Orientation="Vertical" Span="1" />
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="{x:Type models:Workout}">
                        <Frame BackgroundColor="LightBlue" Margin="10" Padding="10" CornerRadius="10">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Label Text="{Binding Name}" FontAttributes="Bold" FontSize="Title" TextColor="DarkBlue" />
                                <Label Text="{Binding Description}" FontSize="Subtitle" TextColor="DarkBlue" Grid.Row="1"/>
                                <Label Text="{Binding Reps, StringFormat='Reps: {0}'}" FontSize="Subtitle" TextColor="DarkBlue" Grid.Row="2"/>
                                <Label Text="{Binding Sets, StringFormat='Sets: {0}'}" FontSize="Subtitle" TextColor="DarkBlue" Grid.Row="3"/>
                            </Grid>
                            <Frame.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodel:WorkoutListViewModel}}, Path=SelectWorkoutCommand}"
                                                  CommandParameter="{Binding .}"/>
                            </Frame.GestureRecognizers>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <!--<Frame x:Name="popupFrame" 
                   IsVisible="{Binding Source={RelativeSource AncestorType={x:Type viewmodel:WorkoutListViewModel}}, Path=IsAddWorkoutPopupVisible}" 
                   BackgroundColor="#2F2F2F"
                   Padding="20" 
                   CornerRadius="10"
                   Margin="100"
                   HorizontalOptions="FillAndExpand"
                   VerticalOptions="FillAndExpand">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
      
                    <Grid Grid.Row="0" Margin="10">
                        <Label Text="Name:" />
                        <Entry Text="{Binding Source={RelativeSource AncestorType={x:Type viewmodel:WorkoutListViewModel}}, Path=Name}" HorizontalTextAlignment="Center"/>
                    </Grid>

                    <Grid Grid.Row="1" Margin="10">
                        <Label Text="Description:" />
                        <Entry Text="{Binding Source={RelativeSource AncestorType={x:Type viewmodel:WorkoutListViewModel}}, Path=Description}" HorizontalTextAlignment="Center"/>
                    </Grid>

                    <Grid Grid.Row="2" Margin="10">
                        <Label Text="Reps:" />
                        <Entry Text="{Binding Source={RelativeSource AncestorType={x:Type viewmodel:WorkoutListViewModel}}, Path=Reps}" HorizontalTextAlignment="Center"/>
                    </Grid>

                    <Grid Grid.Row="3" Margin="10">
                        <Label Text="Sets:" />
                        <Entry Text="{Binding Source={RelativeSource AncestorType={x:Type viewmodel:WorkoutListViewModel}}, Path=Sets}" HorizontalTextAlignment="Center"/>
                    </Grid>
                </Grid>
            </Frame>-->
        </StackLayout>
    </ScrollView>
</ContentPage>
